# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Environment — never commit real keys
.env

# Orange Data Mining temporary/swap files only (keep .ows and .pkcls)
*.ows.swp*

# Jupyter checkpoints
.ipynb_checkpoints/

# MLflow run artifacts (solo la carpeta raíz, no services/mlflow/)
/mlflow/

# IDE
.vscode/
.idea/

# Claude Code internal settings
.claude/

# Los audios de producción van a MinIO via API y nunca tocan disco.
# Los audios de prueba (data/audios/) SÍ se guardan en el repo.
